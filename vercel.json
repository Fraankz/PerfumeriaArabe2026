{
  "buildCommand": "cd apps/web && npm install && npx react-router build",
  "outputDirectory": "apps/web/build/client",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}